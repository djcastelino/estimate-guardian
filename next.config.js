/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Disable PWA for now to fix deployment error
  // Will re-enable after fixing the configuration
};

module.exports = nextConfig;
